# STATE - Random factory checkpoint
- **Updated:** 2026-08-21 (maintainer run 32510561736, EVENT `created` on PR #104, 17:54Z). PR #104 (Prism, issue #103) **MERGED** (rebase, branch preserved) at 17:55:24Z into `main` = `35a2d68`. Issue #103 CLOSED. New-project merges today: **1/2**.

## STANDING OWNER DIRECTIVES (active)
- **Obsidian shipped** (#93 manually merged by owner as orphan root `60748e88`; promoted to Current via merged PR #115).
- **NEXT PRIORITY (owner):** build **Prism (issue #103)** - beat JPEG XL (~3.1 bpp on Kodak). **M0 (foundation) is now MERGED**; the M1-M4 optimization loop (Squeeze + MA-tree `llc_class`/`sibling_class` context model) is the next task when the owner picks it up.
- **One-PR rule + NEVER delete PR branches:** satisfied; #104 branch preserved (merged without `--delete-branch`).
- **Maintainer sovereign-recovery directive:** `recover` of orphaned/closed PRs authorized; `main` must never become a divergent/orphan ROOT.
- **Owner WILL (resolved):** builder model = free `opencode/muse-spark-1.2-contributor-free` (PR #111 MERGED). Paid tier crashes with `APIError: No payment method`. Standing fix confirmed live at `opencode.yml:358`.

## CRITICAL INFRASTRUCTURE STATE
- **`main` = `35a2d68`** (post #104 merge; Prism codec + docs landed). PR #104 branch `opencode/issue103-20260821075928` preserved (descends from main, NOT orphan).
- **opencode.json:** `model` = `opencode/hy3-free` (free), `small_model` = `opencode/mimo-v2.5-free` (free). Both fine.
- **pages.yml:** manually re-triggered after #104 merge (run `32510773918`, in_progress) to deploy the updated root `index.html` (Prism card) + `docs/`.
- **PR #114 (issue #112) - MERGED**; **#112 CLOSED**.
- **PR #102 - CLOSED.** #42 Brainstorm Board unblocked.

## IN FLIGHT
1. **PR #104 (Prism, issue #103) - MERGED at 17:55:24Z, branch preserved.** M0 foundation complete and review/test-passed. M1-M4 optimization loop (Squeeze + MA-tree context model, real Kodak vs JXL 3.1 bpp) remains as the next Builder/Researcher/Architect task. Issue #103 CLOSED. Remaining new-project budget today: 1/2.

## PENDING (in order)
1. **Prism M1-M4 optimization loop:** when the owner directs, route the next phase (Squeeze + MA-tree `llc_class`/`sibling_class` context model, real Kodak bpp benchmarking) through Architect/Builder/Fixer per the milestone map. M0 non-blocking backlog (encode_file stub, crc32_combine misname, predict_sample dead stub, container unused var, real-Kodak byte-cmp wiring) carries into M1.
2. **#42 Board resume:** unblocked (was blocked on #102, now closed); pick from parked candidates now that Prism has shipped.
3. **`lab.yml` Lab Engineer pin bump (`hy3-free`):** escalate to direct edit only if a needed Lab Engineer run no-ops.

## ISSUES
- **#103 (Prism)** - CLOSED (merged via #104).
- **#112 (automatic PR recovery)** - CLOSED (shipped via merged #114).
- **#110 (paid model crash)** - CLOSED (resolved by merged #111).
- **#108 / #109 (model switch)** - CLOSED by #109 merge.
- **#100 (Resonata)** - CLOSED (owner halt); no recover.
- **#42 (Brainstorm Board)** - OPEN; unblocked (was blocked on #102, now closed).
- **#70 (Lab Health)** - Auditor owns daily summary.
- **#102** - CLOSED.
- **#62 (Fix README and website)** - CLOSED (by #63; #115 synced the files).
- **#115 (Obsidian landing sync)** - MERGED.

## REVIEWER/TESTER/MODEL STATUS
- `origin/main` = `35a2d68`. Today's new-project merges: 1/2 (PR #104 Prism; PRs #115/#109/#111/#114 were docs/infra, exempt).
- PR #104: MERGED. No open PRs.
- Build agent (workflow `model:` input): `opencode/muse-spark-1.2-contributor-free` = FREE (fixed).
- `lab.yml` Lab Engineer pin: `opencode/hy3-free` (no-op risk; escalate if needed).
- `maintainer.yml` trigger dispatch FIXED on main (`/oc recover` can post).

## NEXT STEPS
1. Prism M1-M4: when owner directs, route Architect/Builder/Fixer for the Squeeze + MA-tree optimization loop; wire real Kodak + byte-cmp before M3 merge.
2. #42: resume Brainstorm Board picks now that Prism has shipped.
3. `lab.yml` Lab Engineer pin still `hy3-free`: bump if a needed `/oc lab` run no-ops.

## OPEN QUESTIONS
- Prism M1-M4: will the owner open the next issue/PR for the Squeeze + MA-tree optimization (real Kodak vs JXL 3.1 bpp)? Remaining new-project budget 1/2 today.
- #42: unblocked by #102 closing; resume candidates now that Prism has shipped.
- `lab.yml` Lab Engineer pin still `hy3-free`: bump if a needed `/oc lab` run no-ops.
- Superseded orphan PRs (#84/#83/#69/#60): intentionally not recovered.

- Mae, the Maintainer


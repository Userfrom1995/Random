# STATE - Random factory checkpoint
- **Updated:** 2026-08-21 (maintainer run 32515063206, EVENT `/oc maintainer` on issue #42, 18:46Z). Repo idle: no open PRs. PR #104 (Prism M0, issue #103) MERGED at 17:55:24Z; `main`=`35a2d68`. New-project merges today: **1/2** (Prism). This run routes the Architect to draft the M1-M4 optimization blueprint on PR #104. Ideator just refreshed the board (Cartograph/Lyricon/Quartz) but those are parked behind Prism per the owner's standing directive.

## STANDING OWNER DIRECTIVES (active)
- **Obsidian shipped** (#93 manually merged by owner as orphan root `60748e88`; promoted to Current via merged PR #115).
- **NEXT PRIORITY (owner, 07:49Z):** build **Prism (issue #103)** - an upgrade over Obsidian that supports all major input formats and beats JPEG XL (~3.1 bpp on Kodak). M0 (foundation) is MERGED; the M1-M4 optimization loop (Squeeze + MA-tree `llc_class`/`sibling_class` context model) is NOW being architected (this run, `architect` on PR #104). Owner override: no merge of any Prism iteration until M0 + M1 + M2 + M3 are all met bit-exactly on real Kodak.
- **One-PR rule + NEVER delete PR branches:** satisfied; #104 branch `opencode/issue103-20260821075928` preserved (merged without `--delete-branch`).
- **Maintainer sovereign-recovery directive:** `recover` of orphaned/closed PRs authorized; `main` must never become a divergent/orphan ROOT.
- **Owner WILL (resolved):** builder model = free `opencode/muse-spark-1.2-contributor-free` (PR #111 MERGED). Paid tier crashes with `APIError: No payment method`. Standing fix confirmed live at `opencode.yml:358`.
- **Owner "don't get distracted" directive (07:49Z/18:46Z):** Prism is THE priority; board candidates are parked until Prism clears the JXL gate.

## CRITICAL INFRASTRUCTURE STATE
- **`main` = `35a2d68`** (post #104 merge; Prism codec + docs landed). PR #104 branch preserved (descends from main, NOT orphan).
- **opencode.json:** `model` = `opencode/hy3-free` (free), `small_model` = `opencode/mimo-v2.5-free` (free). Both fine.
- **pages.yml:** re-triggered after #104 merge (runs 32510773918 / 32510898951, completed success) - root `index.html` + `docs/` deployed (Prism card live).
- **PR #114 (issue #112) - MERGED**; **#112 CLOSED**.
- **PR #102 - CLOSED.** #42 Brainstorm Board unblocked.
- **PR #115 (issue #62/README+site) - MERGED.** Obsidian promoted to Current; site + README synced.

## IN FLIGHT
- **Prism M1-M4 optimization loop:** this run emitted `architect` on PR #104 to draft the B5-B9 blueprint (predictor bank + residual context -> Squeeze + MA-tree coupling -> CM/LZP). After the Architect's blueprint PR lands, route Builder (B5-B9) -> Reviewer -> Tester (real Kodak vs JXL 3.1 bpp). No merge until M3 met bit-exactly (owner override).

## PENDING (in order)
1. **Prism M1-M4 optimization loop:** Architect blueprint (in flight via `architect` on PR #104) -> Builder (B5-B9) -> Reviewer -> Tester on real Kodak. M0 non-blocking backlog (encode_file stub, crc32_combine misname, predict_sample dead stub, container unused var, real-Kodak byte-cmp wiring) carries into B5.
2. **#42 Board resume (parked):** fresh Ideator batch posted 18:46Z (Cartograph/Dart, Lyricon/Lua, Quartz/Erlang). PARKED behind Prism per owner directive; pick only after Prism clears the JXL gate. Parked earlier candidates (Corundum/Tundra/Ravel/Aether/Nimbus/Penumbra/Vellum) remain eligible after.
3. **`lab.yml` Lab Engineer pin bump (`hy3-free`):** escalate to direct edit only if a needed Lab Engineer run no-ops.

## ISSUES
- **#103 (Prism)** - CLOSED (merged via #104).
- **#112 (automatic PR recovery)** - CLOSED (shipped via merged #114).
- **#110 (paid model crash)** - CLOSED (resolved by merged #111).
- **#108 / #109 (model switch)** - CLOSED by #109 merge.
- **#100 (Resonata)** - CLOSED (owner halt); no recover.
- **#62 (Fix README and website)** - CLOSED (by #63; #115 synced the files).
- **#115 (Obsidian landing sync)** - MERGED.
- **#42 (Brainstorm Board)** - OPEN; fresh batch posted 18:46Z; parked behind Prism.
- **#70 (Lab Health)** - Auditor owns daily summary.

## REVIEWER/TESTER/MODEL STATUS
- `origin/main` = `35a2d68`. Today's new-project merges: 1/2 (PR #104 Prism; PRs #115/#109/#111/#114 were docs/infra, exempt).
- No open PRs. Nothing pending review/test until the Architect's blueprint PR lands.
- Build agent (workflow `model:` input): `opencode/muse-spark-1.2-contributor-free` = FREE (fixed).
- `lab.yml` Lab Engineer pin: `opencode/hy3-free` (no-op risk; escalate if needed).
- `maintainer.yml` trigger dispatch FIXED on main (`/oc recover` can post).

## NEXT STEPS
1. Prism M1-M4: Architect blueprint in flight (`architect` on PR #104). When its blueprint PR lands, route Builder (B5-B9 Squeeze + MA-tree), then Reviewer + Tester on real Kodak; hold merge until M3 (< 8.71 bpp) met bit-exactly per owner override.
2. #42: PARKED - do not pick a board candidate until Prism clears the JXL gate.
3. `lab.yml` Lab Engineer pin still `hy3-free`: bump if a needed `/oc lab` run no-ops.

## OPEN QUESTIONS
- Prism M1-M4: will the Architect's blueprint enable the Builder to land Squeeze + MA-tree and cross under JPEG XL 8.71 on real Kodak? (Owner override: no merge until M3 met.)
- #42: PARKED behind Prism; resume candidate pick only after Prism clears the JXL gate.
- `lab.yml` Lab Engineer pin still `hy3-free`: bump if a needed `/oc lab` run no-ops.
- Superseded orphan PRs (#84/#83/#69/#60): intentionally not recovered.

- Mae, the Maintainer

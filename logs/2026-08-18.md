# 2026-08-18 - Maintainer run 32152812476 (owner `/oc maintainer` on PR #83)

## State snapshot (start of run)
- PR #83 OPEN, head `977398e0c1d32d556ffda62426345b00be0b4daf`, `mergeable: CONFLICTING`, **no common ancestor with `main` (`e4e33928`)** - `git merge-base` returns EMPTY. Single canonical Obsidian PR; branch `opencode/issue68-20260818070512`.
- CMARC stack R1->R2.4 built, all OFF by default; production 10.16 bpp (PNG MET). Corrected R3 blueprint on branch (`b6f67d04`).
- Builder `continue` run `32151973192` completed (~8m35s, ended 15:10Z): landed `run_kodak.sh` self-provisioning hardening + began the `cmarc_residual_ctx` env seam; left off mid-R3 ("R3 needs env seam; checking encode entry point").
- Measurement blocker OPEN: `data/kodak/*.ppm` NOT in git (git-ignored by design); reference codec toolchain + no-auth Kodak mirror uncertain in CI.

## Decisions + rationale
- **Decision list: `[{"action":"continue","pr":83}]`.**
  - Resumed the Builder on the single branch to (1) durably repair the orphan-`main` break by rebasing `opencode/issue68-20260818070512` onto `origin/main` and force-pushing the SAME branch (no new PR - one-PR rule absolute), and (2) implement the corrected R3 (R3-B Rice-through-binary + neutral `CMARC_PRIOR=2048`, then R3-A residual DIFF context, R3-C, R2.4) and re-measure on real Kodak (or honest synthetic if PPMs unreachable).
  - Did NOT fire `factory`: the Factory is the wrong tool for project data AND its squash-rebase (`f1788472`) is what re-orphaned `main` (parallel history, duplicate-commit-SHA divergence). Re-dispatching it risks another redundant PR / re-orphaning. The Builder owns the branch rebase + obsidian data reproducibility.
  - Did NOT fire `architect`/`research`: the corrected R3 blueprint is already on the branch and current; the bottleneck is now implementation + reproducible measurement, not design.
  - Did NOT merge: gates unmet (best reproducible real-Kodak 10.0906 bpp > WebP 9.61 / JPEG XL 8.71) AND measurement still not durably reproducible. Owner override #2 stands.
- Wrote PR #83 comment: status summary + the orphan-main root cause (Factory parallel squash-rebase) + ordered Builder instructions + gate status.

## Callbacks / actions taken
- None beyond writing the five deliverable files. No `/oc` posted by Mae (hardcoded step posts owner triggers).

## Run link
- This run: https://github.com/Userfrom1995/Random/actions/runs/32152812476
- Builder `continue` (just completed, informed this decision): https://github.com/Userfrom1995/Random/actions/runs/32151973192

## Open questions / carry-forward
- Will the Builder's rebase + force-push make PR #83 rebase-mergeable (non-empty `merge-base`, `gh pr view` MERGEABLE) without opening a new PR? Verify next survey.
- Will corrected R3 actually beat 9.61 on real Kodak? Awaits reproducible measurement; first R3 regressed due to the prior/budget bug now corrected.
- Can the Builder reproduce exact Kodak PPMs in CI (network + no-auth mirror + reference codecs)? If unreachable, report honest synthetic number, never fake data.
- README.md / index.html Obsidian promotion still pending (deferred quiet run).
- One-PR integrity: #83 sole canonical Obsidian PR; Builder pushes to it, never opens a codec PR.

- Mae, the Maintainer
<!-- run: 32152812476 -->

---

# 2026-08-18 - Maintainer run 32156121439 (owner `/oc maintainer` on PR #83)

## State snapshot (start of run)
- PR #83 OPEN, head `206781fc` (advanced from `977398e0`), `mergeable: CONFLICTING`, **no common ancestor with `main` (`e4e3392`)** - `git merge-base` empty; `main` is NOT an ancestor of the branch. Single canonical Obsidian PR; branch `opencode/issue68-20260818070512`.
- Builder `continue` run `32153177489` (15:13Z, conclusion `success`) pushed head `206781f` but posted **no PR comment** (silent-stall pattern - likely timed out at the comment step). Its real output is on the branch + progress file.
- **Decisive new finding (Builder run 32153177489):** the shared 16-bit binary range coder (`BinModel` WNC probabilities in `rans.rs`) is lossless but **EXPLODES on real Kodak residuals** - CMARC forced = kodim01 27.3, kodim02 25.5, kodim03 21.6 bpp vs GR 10.09. R3-B reverted to blueprint-exact unary and still exploded (25.7 bpp); R3-A scaffolding safe/off-by-default. This means every "best config" number we ever quoted (10.09, 10.16) came *entirely* from the never-expand safety net falling back to v1 GR - CMARC has never actually beaten GR on real Kodak. The blueprint's "H(p)+epsilon" assumption is false; the binary coder's probability adaptation is broken. Progress file explicitly says this is a Researcher/Architect task, not a Builder task.

## Decisions + rationale
- **Decision list: `[{"action":"research","pr":83}]`.**
  - Re-engaged the **Researcher (Mode 2)** on the single existing PR to diagnose *why the binary range coder explodes on real photographic residuals* (WNC prior strength, adaptation rate on sparse contexts, or a lockstep/normalization bug in `RangeEnc`/`RangeDec`) and to prescribe a correct context-modeled adaptive arithmetic coder (QM-class or correct adaptive rANS) that reaches `H(p)+epsilon` on real Kodak - not just synthetic near-Laplacian data.
  - Did NOT fire `continue`: the Builder is blocked by a core algorithmic bug it localized but cannot resolve (per the progress file's own recommendation). Re-poking R3 would not unblock it.
  - Did NOT fire `architect` yet: the Researcher must first diagnose the root cause; the Architect then re-blueprints the corrected coder on the same PR.
  - Did NOT fire `factory`: it is the wrong tool for project data and its prior squash-rebase is what re-orphaned `main`.
  - Did NOT merge: gates unmet. CMARC itself does not beat GR on real Kodak (it explodes); GR fallback 10.0906 bpp > WebP 9.61 / JPEG XL 8.71. Owner override #2 stands.
- Wrote PR #83 comment: root-cause bug disclosure + research re-engagement + orphan-main re-flag + gate status.

## Callbacks / actions taken
- None beyond writing the five deliverable files. No `/oc` posted by Mae (hardcoded step posts owner triggers).

## Run link
- This run: https://github.com/Userfrom1995/Random/actions/runs/32156121439
- Builder `continue` (informed this decision, discovered the coder bug): https://github.com/Userfrom1995/Random/actions/runs/32153177489

## Open questions / carry-forward
- **Why does the 16-bit binary range coder explode on real Kodak (21-27 bpp) while staying lossless?** Awaits the Researcher.
- Will the Researcher/Architect deliver a correct adaptive arithmetic coder that reaches JPEG-LS-class (9.71) or better on real Kodak? The predictor is sound (same LOCO-I GAP as JPEG-LS); the entropy backend is the proven bottleneck.
- Can the Builder reproduce exact Kodak PPMs in CI (network + no-auth mirror + reference codecs)? If unreachable, report honest synthetic number, never fake data.
- Orphan-main repair: the Builder's `continue` did NOT rebase+force-push; must do so before the eventual merge (verify next survey: non-empty `merge-base`, `gh pr view` MERGEABLE).
- README.md / index.html Obsidian promotion still pending (deferred quiet run).
- One-PR integrity: #83 sole canonical Obsidian PR; Builder pushes to it, never opens a codec PR.

- Mae, the Maintainer
<!-- run: 32156121439 -->

---

# 2026-08-18 - Maintainer run 32156541447 (owner `/oc research` + `/oc maintainer` on PR #83)

## State snapshot (start of run)
- PR #83 OPEN, head `206781fc2719a3fc39fee9787fa6e662a4637343`, `mergeable: CONFLICTING`, **no common ancestor with `main`** - `git merge-base origin/main opencode/issue68-20260818070512` returns EMPTY. Single canonical Obsidian PR; branch `opencode/issue68-20260818070512`.
- CMARC stack (R1 -> R2.4) + R3 (R3-A residual-context + R3-B neutral-prior Rice) built on-branch, all OFF by default. Production unchanged at **10.16 bpp** (PNG 13.05 MET). WebP 9.61 + JPEG XL 8.71 PENDING and, to date, UNMET.
- **Root-cause bug stands (from Builder run `32153177489`):** the shared 16-bit binary range coder (`BinModel` WNC probabilities in `rans.rs`, used by `RangeEnc`/`RangeDec` (CMARC) AND `BinEnc`/`BinDec` (M3-A LZ)) is lossless but **EXPLODES on real Kodak residuals** (CMARC forced = 21-27 bpp vs GR 10.09). Every "best config" number quoted (10.09, 10.16) came entirely from the never-expand safety net falling back to v1 GR - CMARC has never beaten GR on real Kodak. The blueprint's "H(p)+epsilon" assumption is false; the binary coder's probability adaptation is broken.
- **Silent research failure observed:** my prior run (32156121439, 15:42Z) decided `research` -> the hardcoded step posted `/oc research` (owner 15:46:48Z) -> `opencode.yml` research job `32156521813` concluded `success` in **13s** but committed **no diagnostic doc and posted no bot comment**. It was masked by `continue-on-error: true` in `opencode.yml`. So the research was never actually performed.

## Decisions + rationale
- **Decision list: `[{"action":"research","pr":83}]`.**
  - Re-engaging the **Researcher (Mode 2)** on the single existing PR to diagnose *why the binary range coder explodes on real photographic residuals* (WNC prior strength, adaptation rate on sparse contexts, or a lockstep/normalization bug in `RangeEnc`/`RangeDec`/`BinEnc`/`BinDec`) and to prescribe a correct context-modeled adaptive arithmetic coder (QM-class or correct adaptive rANS) that reaches `H(p)+epsilon` on real Kodak - not synthetic near-Laplacian proxies.
  - Re-firing `research` (not `continue`): the earlier research trigger silently failed with no deliverable, so it must be re-run; the Builder is blocked by a core algorithmic bug it localized but cannot resolve (per the progress file). The Researcher forwards to the Architect (`/oc architect`), who re-blueprints the corrected coder on the same PR, then the Builder resumes via `continue`.
  - Did NOT fire `architect` yet: the Researcher must first pinpoint the exact bug; the Architect then re-blueprints on the same PR.
  - Did NOT fire `factory`: wrong tool for the coder algorithm, and its prior squash-rebase is what re-orphaned `main`. (However: the 13s research-crash is an infra symptom - if research fails a second time I will flag it to the Factory for model/infra fallback per the model-management policy.)
  - Did NOT merge: gates unmet. Best reproducible real-Kodak = 10.0906 bpp (GR fallback) > WebP 9.61 / JPEG XL 8.71. Owner override #2 stands.

## Callbacks / actions taken
- None beyond writing the five deliverable files. No `/oc` posted by Mae (hardcoded step posts owner triggers).

## Run link
- This run: https://github.com/Userfrom1995/Random/actions/runs/32156541447
- Prior maintainer run (decided `research`): https://github.com/Userfrom1995/Random/actions/runs/32156121439
- Builder `continue` (discovered the coder bug): https://github.com/Userfrom1995/Random/actions/runs/32153177489
- Silently-failed research run (13s, no deliverable): https://github.com/Userfrom1995/Random/actions/runs/32156521813

## Open questions / carry-forward
- **Why does the 16-bit binary range coder explode on real Kodak (21-27 bpp) while lossless?** Awaits the Researcher (re-engaged this run). If the re-fired research also silently fails (13s no-op), forward directly to the Architect with the existing empirical diagnosis and flag the infra crash to the Factory.
- Will the Researcher/Architect deliver a correct adaptive arithmetic coder that reaches JPEG-LS-class (9.71) or better on real Kodak? The predictor is sound (same LOCO-I GAP as JPEG-LS, which hits 9.71 bpp); the entropy backend is the proven bottleneck.
- Can the Builder reproduce exact Kodak PPMs in CI (network + no-auth mirror + reference codecs)? If unreachable, report honest synthetic number, never fake data.
- Orphan-main repair: PR #83 `mergeable: CONFLICTING`, no merge base with `main`. The Builder must eventually rebase `opencode/issue68-20260818070512` onto `origin/main` and force-push the SAME branch (no new PR) so `--rebase` merge becomes possible. Deferred (gate unmet).
- README.md / index.html Obsidian promotion still pending (deferred quiet run).
- One-PR integrity: #83 sole canonical Obsidian PR; Builder pushes to it, never opens a codec PR.

- Mae, the Maintainer
<!-- run: 32156541447 -->

---

# 2026-08-18 - Maintainer run 32157692174 (owner `/oc maintainer` on PR #83)

## State snapshot (start of run)
- PR #83 OPEN, head `f506050f244a931d8125ef372a498a4051ea870e`, `mergeable: CONFLICTING`, **no common ancestor with `main`** - `git merge-base origin/main opencode/issue68-20260818070512` returns EMPTY. Single canonical Obsidian PR; branch `opencode/issue68-20260818070512`.
- CMARC stack (R1 -> R2.4) + R3 (R3-A residual-context + R3-B neutral-prior Rice) built on-branch, all OFF by default. Production unchanged at **10.16 bpp** (PNG 13.05 MET). WebP 9.61 + JPEG XL 8.71 PENDING and, to date, UNMET.
- **Root-cause bug, now DIAGNOSED (Researcher run `32156932119`, finishing ~15:58Z):** the shared 16-bit binary range coder is **lossless and passes near-entropy tests** - the *core* is NOT broken. The real defect is **model adaptation**: the per-(cid,bin) `BinModel` WNC probabilities cost ~0.97-1.88 bits per bin instead of ~1, so CMARC emits 3.3-5.4x true entropy on Laplacian residuals (b=2: 18.97 bpp vs 3.53 entropy). Every "best config" number quoted (10.09, 10.16) came entirely from the never-expand safety net falling back to v1 GR - CMARC has never beaten GR on real Kodak. The blueprint's "H(p)+epsilon" assumption failed because the WNC adaptation never converges.
- **Misleading test cleaned up:** the Researcher also found `binenc_vs_rangeenc_skew` is a **malformed test** (RNG compares an `f64 < 0.9` against a large integer → all-1s stream) - that failure was a red herring, not evidence of coder rot. The other range-coder tests pass.

## Decisions + rationale
- **Decision list: `[{"action":"architect","pr":83,"head":"f506050f244a931d8125ef372a498a4051ea870e"}]`.**
  - Engaging the **Architect (Mode 2)** on the single existing PR #83 to re-blueprint the corrected entropy backend, now that the Researcher pinpointed the defect: a **per-context adaptive arithmetic/range coder whose probability model actually converges to H(p)** (either a correctly-tuned fast/low-prior WNC reaching ~1 bit/bin, a QM-class context-modelled coder, or a correct adaptive rANS), integrated with the LOCO-I GAP predictor + residual DIFF context. The Architect delivers the blueprint on the same branch; the Builder then resumes via `continue` and re-measures on REAL Kodak. No second PR (owner override #1).
  - Did NOT fire `continue`: the Builder is blocked by a core algorithmic bug the Researcher localized (per-bin model cost ~1.88 bits) - re-poking the existing CMARC would not unblock it; the coder needs a redesign first.
  - Did NOT re-fire `research`: the diagnosis is in hand (run `32156932119` succeeded with a concrete, test-backed finding). Forward to the Architect as the state machine prescribes.
  - Did NOT fire `factory`: the coder fix is algorithmic, not infra; orphan-main (git history) is deferred because the gate is unmet, and the Factory's prior squash-rebase is what re-orphaned `main`.
  - Did NOT merge: gates unmet. Best reproducible real-Kodak = 10.0906 bpp (GR fallback) beats optipng PNG (13.05) but is still above WebP (9.61) and JPEG XL (8.71). Owner override #2 stands.

## Callbacks / actions taken
- None beyond writing the five deliverable files. No `/oc` posted by Mae (hardcoded step posts owner triggers).

## Run link
- This run: https://github.com/Userfrom1995/Random/actions/runs/32157692174
- Researcher run (delivered root-cause diagnosis): https://github.com/Userfrom1995/Random/actions/runs/32156932119
- Prior maintainer run (re-fired research): https://github.com/Userfrom1995/Random/actions/runs/32156541447
- Builder `continue` (discovered the coder explosion): https://github.com/Userfrom1995/Random/actions/runs/32153177489

## Open questions / carry-forward
- **Will the Architect's corrected coder reach JPEG-LS-class (9.71) or better on real Kodak?** The predictor is sound (same LOCO-I GAP as JPEG-LS); the entropy backend is the proven bottleneck - now localized to WNC model adaptation (per-bin cost ~1.88 bits instead of 1).
- Can the Builder reproduce exact Kodak PPMs in CI (network + no-auth mirror + reference codecs)? If unreachable, report honest synthetic number, never fake data.
- Orphan-main repair: PR #83 `mergeable: CONFLICTING`, no merge base with `main`. The Builder must eventually rebase `opencode/issue68-20260818070512` onto `origin/main` and force-push the SAME branch (no new PR) so `--rebase` merge becomes possible. Deferred (gate unmet).
- Malformed test: `binenc_vs_rangeenc_skew` has an RNG bug (false failure) - fix once the coder is reworked (Builder/Fixer task, low priority).
- README.md / index.html Obsidian promotion still pending (deferred quiet run).
- One-PR integrity: #83 sole canonical Obsidian PR; Builder pushes to it, never opens a codec PR.
- opencode.yml silent-failure masking: the research job's `continue-on-error: true` let a 13s no-deliverable run pass as `success`; should be guarded so a missing decision.json fails the run (Factory task, lower priority).

- Mae, the Maintainer
<!-- run: 32157692174 -->
<!-- run: 32157692174 -->
<!-- run: 32159973855 -->

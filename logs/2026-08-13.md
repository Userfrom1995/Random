# 2026-08-13 — Mae: Granite complete → review; reviewer pipeline down on billing

Run: https://github.com/Userfrom1995/Random/actions/runs/31736523812 (issue_comment on PR #50 — `/oc maintainer` by the owner, 19:34:29Z)

## State snapshot

- **PR #50 (Granite, `opencode/issue47-20260813185035`)** — OPEN, bot-authored,
  head `84f370df` (9 commits), **MERGEABLE/CLEAN** on latest main. Progress file
  `Status: complete` (updated 19:30:00Z). Builder's "Build complete" comment
  (19:34:27Z): 81 tests all green, `go build`/`go vet`/`go test` clean, docs +
  docs site + ideas entry, landing promoted (Orrery moved to previous projects),
  `Closes #47`. **`reviews: []`, no `/oc review` posted yet** — this is the first
  trigger. The `/oc continue` (19:19:07Z) from the previous run was picked up by
  build run `31735229831` (success).
- **PR #49 (pages fix)** — OPEN, bot-authored, head `a27eae7d`, MERGEABLE/CLEAN,
  1 commit. Owner's `/oc review (head a27eae7d…)` at 19:07:39Z triggered review
  run `31734266970` — which **CANCELLED at 19:22:55Z with no verdict**. Logs show
  the reviewer model call failed repeatedly: `AI_APICallError: No payment method.
  Add a payment method here: https://opencode.ai/workspace/wrk_01KZGB6N9Y8R8DK6THMA0SD1TZ/billing`
  (CreditsError) at 19:07:59Z, then 504/524 stream errors at 19:13/19:18, then
  the step cancelled after ~15 min. **No `/oc approve`, no `/oc fix` — nothing.**
- **The reviewer model is the blocker:** `opencode-review.yml:57` uses
  `opencode/nemotron-3-ultra-free`, which now demands payment on the workspace.
  The builder/maintainer/ideate models (`deepseek-v4-flash-free`) work fine. So
  the whole review gate (and therefore merge) is currently blocked at the model
  level, for #49 and any future PR including #50.
- main is at `d55b6493` ("chore: refine hosting rules and enforce modular
  commits", 19:20:06Z, direct). Pages deploy green on it and on PR #50 preview.
- Issues open: #47 (Granite), #48 (pages fix), #42 (board; only Gambit remains,
  unreacted).

## Decision + rationale

- **`review` on PR #50 (head `84f370df`)** — bot PR, work complete, no
  auto-review on main (`opencode-pr-trigger.yml` only posts for human PRs), and
  no existing `/oc review` for this head. This is the standard trigger. Even
  though the reviewer model is currently broken, the trigger must exist so the
  round runs the moment the model/billing is fixed; I flag the blocker in the
  comment.
- **`ping` on PR #49** — its review round was cancelled by the infrastructure
  error, not a verdict; there are no findings. A short bot note explains that so
  nobody mistakes the silence for a clean pass or a rejection, and so the owner
  knows #49 needs a re-round (rerun of `31734266970` or a fresh trigger) once
  the reviewer model is fixed.
- **No `review` re-emit for #49** — the owner's `/oc review (head a27eae7d…)`
  trigger already exists; re-issuing would be deduped/wasted. The fix is
  upstream (model/billing), not another trigger.
- No merge (no approval), no continue (both builds complete), no fix (no
  findings), no build (nothing new to start), no ideate (board not thin: Gambit).

## Callbacks

- `/oc review (head 84f370df…)` on PR #50 (via decision `review`).
- Plain bot ping on PR #49 (via decision `ping`).
- Public bot comment on PR #50 (via comment.md): Granite complete + the review
  pipeline billing blocker, with the two options for the owner.

## Open questions

- **Will the owner fix the reviewer model/billing?** This is the single point
  of failure for the whole factory right now — no reviewer, no merge. Options
  flagged: add a payment method to the workspace, or switch
  `opencode-review.yml`'s model to a different working one (keeping the
  two-model loop). If the owner goes the direct-main route again, the fix can
  land in minutes.
- Once fixed: #49 needs a rerun/re-trigger (run `31734266970` is cancelled with
  no verdict), #50's fresh `/oc review` will then be able to run.
- Next board pick (only Gambit left) after #49/#50 clear.

## Note to future Mae

The billing error on `nemotron-3-ultra-free` is the headline. Keep an eye on
whether the owner patches `opencode-review.yml` (direct or PR) or adds payment.
On `/oc approve` of #49 → merge + close #48; on `/oc approve` of #50 → merge +
close #47. Also: the Orrery `pages.yml` staging tail is fully subsumed by the
auto-stage rewrite — no separate block needed, do not keep flagging it.
`personality.md` unchanged.
<!-- run: 31736523812 -->
<!-- run: 31736523812 -->

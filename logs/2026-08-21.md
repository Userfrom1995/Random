# 2026-08-21 — Maintainer log (run 32484038135, EVENT: created on PR #93, owner recovery-strategy question 12:53:05Z)

## State snapshot (start of run)
- Trigger: `maintainer` workflow, EVENT `created` on PR **#93** (Obsidian, now CLOSED). Owner comment at 12:53:05Z asked: (1) would the Maintainer be able to recover a closed-PR/orphan-branch incident if granted `gh` + PR/issue-creation powers, and (2) what concrete recovery mechanism is needed so this never requires a human. Run link:
  https://github.com/Userfrom1995/Random/actions/runs/32484038135
- **PR #93 is CLOSED**, but its branch `opencode/issue68-20260818070512` still exists at `d6fbd1cd` (25 commits) - work was never lost. Owner manually merged the orphan history (`0eb9de0f`); Obsidian codec (9.5209 bpp) is in `main`. Issue #68 CLOSED.
- `main` = `9aeea30` (post #109 merge). Build pin still BROKEN: `opencode/muse-spark-1.2` (PAID) at `opencode.yml:358` -> every Builder run crashes `No payment method`. Free tier `muse-spark-1.2-contributor-free` is the correction target (Lab Engineer dispatched in run 32483441284, still in flight).
- Today's new-project merges: 0/2 (Prism not built; #109 was infra).

## Re-survey findings (fresh, this run)
- Read the referenced incident comment (PR #93 #5365789168): root causes = (1) PR closed not merged + recorded head GC'd -> `gh pr reopen` impossible; (2) `main` was single-root orphan -> no common ancestor -> `gh pr merge --rebase` impossible; (3) Maintainer hard-forbidden from opening PRs/pushing branches -> could not open continuation PR; (4) runaway `/oc fix` loop on the dead PR (fixed by #95/#97/#99).
- Confirmed via `git ls-remote` + `git merge-base`: branch `opencode/issue68-20260818070512` exists at `d6fbd1cd`; `merge-base origin/main <branch>` is EMPTY (orphan) - consistent with the incident. The branch is preserved; only landing was blocked.
- No existing `recover` mechanism in `.github/` (grep: zero hits). STATE.md referenced a "lower-priority `recover.py`" that was never built. So recovery is currently 100% human-dependent.
- Open PRs: only #104 (Prism, docs-only, blocked on paid model). Open issues: #103 (Prism), #112 (NEW recovery, this run), #70 (Lab Health), #42 (Brainstorm Board).

## Actions taken THIS run
- **ANSWERED the owner (comment.md on PR #93):** YES, recovery is possible with `gh` + PR/issue powers; the branch was never lost; the only blocker was the "Maintainer cannot open PRs/push branches" rule. Summarized the four root causes and the four-part mechanism.
- **Created issue #112** "lab: automatic PR recovery mechanism" (bot-authored; the duplicate #113 was closed). The owner explicitly authorized opening issues/PRs + running `gh`, which overrides the safety-net constraint for recovery work. Logged the override.
- **Dispatched `{"action":"lab","issue":112}`** to build the self-healing `recover` mechanism:
  - A. `recover` agent + `recover.yml` (trigger `/oc recover` + auto-detect closed build PR whose branch advanced): resolve current tip, `merge-base`; if non-empty open continuation PR `Refs #N` (one-PR rule) or reopen; if orphan, re-link via orphan-main protection (cherry-pick project commits, force-with-lease) then open PR.
  - B. Branch-preservation tag `recover/<pr>` on every build push (survives GC'd PR head).
  - C. Scoped Maintainer `recover` exception in `maintainer.md` + `opencode.yml` hardcoded trigger step (continuation PR for in-flight work ONLY, never new projects).
  - D. `main` orphan-root prevention (bot run never resets `main` to single orphan root; `recover` always rebases onto current `main`).
- Did NOT touch PR #93 itself: its work is already in `main`, nothing to recover.
- Did NOT emit `continue`/`build` for Prism #104: still blocked on the paid build model; deferred until the free-model PR (run 32483441284) merges.

## Callbacks / actions made
- `gh issue view 93` -> CLOSED; `gh pr view 93` -> CLOSED, head `e184c3c` (GC'd), branch `opencode/issue68-20260818070512`.
- `git ls-remote origin` -> branch `opencode/issue68-20260818070512` = `d6fbd1cd` (exists, 25 commits). `git merge-base origin/main origin/opencode/issue68-20260818070512` -> EMPTY (orphan, as expected).
- `gh api issues/comments/5365789168` -> read the full incident postmortem; confirmed the four root causes.
- `gh issue create` -> #112 (then a duplicate #113 from a retry after an assignee error; #113 closed as duplicate). #112 OPEN, canonical.
- `grep -rn recover .github/` -> zero hits (no existing mechanism).

## Run links
- This run: https://github.com/Userfrom1995/Random/actions/runs/32484038135
- Owner recovery question (PR #93, 12:53:05Z): https://github.com/Userfrom1995/Random/pull/93#issuecomment-5370031425
- Incident postmortem referenced: https://github.com/Userfrom1995/Random/pull/93#issuecomment-5365789168
- Issue #112 (recovery mechanism, Lab Engineer dispatched): https://github.com/Userfrom1995/Random/issues/112
- PR #93 (Obsidian, CLOSED; work in main): https://github.com/Userfrom1995/Random/pull/93
- PR #104 (Prism, BLOCKED on paid model): https://github.com/Userfrom1995/Random/pull/104
- Prior model-fix dispatch (run 32483441284): https://github.com/Userfrom1995/Random/actions/runs/32483441284

## Owner will / loop note (recorded)
- Owner 12:53:05Z: asked for a concrete recovery strategy rather than stopping when something unexpected happens, and offered `gh` + PR/issue-creation powers. I confirmed recovery is automatable and commissioned the mechanism via #112 (`lab`). The prior paid-model saga (#108/#109) and the orphan-main protection procedure in `maintainer.md` are the foundation the `recover` mechanism reuses.
- Override note: creating issue #112 and dispatching `lab` for recovery is authorized by the owner's explicit grant this run; this is a documented exception to the "Maintainer never creates issues/PRs" safety net, scoped strictly to recovery of in-flight work.

## Open questions
- Will the #112 `lab` dispatch execute the recovery mechanism, or no-op on `hy3-free` (requiring direct-edit escalation per the model-management policy)?
- After #112's infra PR merges: does a simulated closed/orphan build PR auto-recover into a new open PR with no human action?
- Will `muse-spark-1.2-contributor-free` execute + push C++ (agentic), or fall back to `nemotron-3-ultra-free`?
- After free model lands + `continue` #104: can the Builder hit M0 fuzz gate and beat JXL 3.1 bpp on Kodak?
- #102: owner to grant `workflows` scope to `OPENCODE_PAT` or merge manually; its `ideate.yml` change still needed for #42.
- Superseded orphan PRs (#84/#83/#69/#60): intentionally not recovered.

- Mae, the Maintainer
<!-- run: 32484038135 -->
<!-- run: 32484038135 -->

# Obsidian - benchmark results

This folder holds the reproducible Kodak benchmark results for every Obsidian
iteration and the pinned reference baseline. See
`../docs/benchmark-methodology.md` for the protocol.

## Reference baseline (pinned toolchain)

Pending: to be computed by the Builder on a pinned machine with pinned tool
versions (cjxl, cwebp, optipng, pngcrush, CharLS, flif, ImageMagick) once the
benchmark harness exists. Literature ranges for orientation (mean bpp on
Kodak): PNG optimized ~4.2, JPEG-LS ~3.7, WebP lossless ~3.4-3.5, FLIF ~3.1,
JPEG XL lossless ~3.1-3.3, MRP ~2.6-2.8.

## Iteration log

| Date | Version | Kodak mean bpp | vs WebP | vs JPEG XL | Encode ms | Decode ms | Notes |
|---|---|---|---|---|---|---|---|
| - | - | - | - | - | - | - | (first row added by the Builder) |

- Dr. Ada, the Researcher